## Plan: Open Tagged GitHub Issues from tasks.md

Create one GitHub issue per ## row in tasks.md (not per bullet sub-line), preserving row tags as GitHub labels and aggregating all sub-lines into the issue body. Use milestone structure so MVP rows are clearly separated from Beyond MVP rows. The workflow is: parse and normalize rows -> ensure labels/milestones exist -> create issues with structured bodies -> verify results and report issue links.

**Steps**
1. Parse ## rows from /Users/jamespjh/devel/inaugural/tasks.md into a normalized table with fields: section (MVP/Beyond MVP), row title, row-level tag set (agent/human/learning/agent+human), and aggregated bullet notes from that row. This row table is the source of truth for issue generation.
2. Define deterministic mapping rules from task metadata to GitHub issue metadata: title format, label mapping (including combined agent+human), milestone mapping (MVP tasks -> MVP milestone; Beyond MVP tasks -> separate label or milestone), and default body template with acceptance criteria placeholders.
3. Preflight repository state using GitHub CLI read calls: confirm repository target, fetch existing labels, fetch milestones, and check for likely duplicate issues by title similarity. Mark any collisions for skip-or-update behavior. Depends on step 1.
4. Reconcile taxonomy in GitHub: create any missing labels (agent, human, learning, MVP, beyond-mvp) and create the MVP milestone if absent. Parallel with step 3 after task parsing is complete.
5. Create issues for all MVP rows (one per ## row), applying row tags as labels, embedding each row's bullet sub-lines into the issue body, and assigning the MVP milestone. Depends on steps 3-4.
6. Create issues for all Beyond MVP rows (one per ## row), applying row tags as labels, embedding each row's bullet sub-lines into the issue body, and applying the separate scope marker (label and/or milestone). Depends on steps 3-4.
7. Post-creation verification: list created/updated issues, validate label and milestone assignments, and spot-check issue bodies for formatting consistency. Depends on steps 5-6.
8. Deliver a concise report with issue numbers/URLs grouped by MVP and Beyond MVP, plus any skipped duplicates and follow-up recommendations.

**Relevant files**
- /Users/jamespjh/devel/inaugural/tasks.md — authoritative task list and tags to convert into issues.
- /Users/jamespjh/devel/inaugural/.github/copilot-instructions.md — confirms repository-level working conventions and boundaries while running automation.

**Verification**
1. Run GitHub CLI list queries after creation to confirm issue count matches parsed ## row count (MVP + Beyond MVP).
2. Validate every created issue includes expected labels based on task tags.
3. Validate all MVP issues are attached to MVP milestone.
4. Validate Beyond MVP issues carry the dedicated scope marker (label and/or milestone).
5. Confirm no accidental duplicates by checking title collisions in open issues.

**Decisions**
- One issue per ## row in tasks.md (not per bullet sub-line).
- Include Beyond MVP tasks now.
- Represent tags via GitHub labels matching task taxonomy.
- Create/use milestone MVP for MVP tasks.
- Included scope: issue creation, labeling, milestone assignment, duplicate checks, and reporting.
- Excluded scope: implementing the task work itself, re-prioritizing task content, and rewriting task descriptions beyond issue-ready phrasing.

**Further Considerations**
1. Recommended title convention: imperative phrase preserving task wording for easy traceability to tasks.md.
2. Recommended body template: context, definition of done, dependencies, and notes from tasks.md so contributors can execute without reopening the source file.
3. If duplicate issues already exist, prefer adding missing labels/milestones and posting a linking comment instead of creating another issue.
