# Plan: Address PR #35 re-review feedback

Date: 2026-04-10

## Context
@jamespjh requested a re-review of PR #35 (array abstraction and benchmarking code from stubs).
Several reviewer comments needed addressing after the initial round of fixes.

## Analysis of Unresolved Comments

### Already addressed in previous commits (5cb98a8 → 18387d2):
- `benchmark.py:55-60`: `torch-gpu` now routes to `timeit_torch_cuda()` using proper CUDA sync ✓
- `array_abstraction.py:133-134`: Error message updated to show actual constraint ✓
- `array_abstraction.py:168-169`: `random_python_matrix()` dimension ordering fixed ✓
- `array_numba.py:2-8`: Now uses `np.random.random()` instead of private Numba API ✓
- `pyproject.toml:20-27`: torch/numba moved to optional extras ✓
- `array_abstraction.py:129-149`: `array()` handles python/numba cases before calling `self.np.array()` ✓
- `engine_support.py:103`: Replaced list comprehension with for loop ✓
- `entry.py:109`: Updated comment ✓
- `benchmark.py:29-31`: JAX timing uses `jax.block_until_ready()` ✓

### Still needed (this session):
1. `engines.py:8`: Add `torch-cpu` to `DONT_TEST_ENGINES` ✓
2. Fix cascading issue: `array_abstraction.py` still has `import jax` at module level → breaks when jax not installed ✓
3. Fix test_scenarios.py: unused module-level `jax_factory` call crashes without jax ✓
4. Fix test_integrator.py: hardcoded `jax-cpu` in parametrize list crashes without jax ✓

## Changes Made

1. **`demo/tests/teachgrav/engines.py`**: Added `torch-cpu` to `DONT_TEST_ENGINES` set
2. **`demo/src/teachgrav/array_abstraction.py`**: Removed top-level `import jax`; moved lazy imports into `configure_jax()`, `array()`, and `random_array()` methods
3. **`demo/tests/teachgrav/test_scenarios.py`**: Removed unused `jax_factory` module-level variable; made jax-cpu seed tests conditional on jax availability
4. **`demo/tests/teachgrav/test_integrator.py`**: Made jax-cpu single-body test case conditional on jax availability
