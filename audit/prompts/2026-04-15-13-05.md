# Prompt Record: Implement and commit FPS wiring step (2026-04-15)

## User instruction summary

The user requested implementation of steps 1-5 from the previously agreed unified FPS plan, then asked to commit this step and include the whole plan and prompt records in the commit artifacts.

## What was requested

1. Add a global FPS control (`--fps`, default 20).
2. Route FPS through both standard animation and convergence video paths.
3. Keep work scoped to early wiring steps only, not interpolation/upsampling logic yet.
4. Commit this step with repository-required metadata artifacts:
   - A timestamped `plans/*.md` file with the full plan.
   - A timestamped `prompts/*.md` file capturing the conversation and decisions.

## Key decisions and reasoning

1. **Single source of truth for frame rate**
   - The previous convergence path referenced a non-CLI `convergence_fps` fallback.
   - Replacing this with `args.fps` unifies behavior and prevents divergent defaults.

2. **Preserve behavior outside fps plumbing**
   - No change to convergence frame-count semantics in this step.
   - No change to integration method/dt/until forwarding.
   - No change to bounding-box or true-law overlay logic.

3. **Minimal-call-path edits**
   - `entry.py`: add CLI arg and thread `args.fps` to both render paths.
   - `viz.py`: accept fps in `visualize()` and `animate()`; remove hardcoded value.
   - tests: update fake args and parser assertions for the new field.

4. **Focused test validation over full suite**
   - Executed targeted tests that directly exercise the changed behavior:
     - `tests/teachgrav/test_convergence_video.py`
     - `tests/teachgrav/test_viz.py`
   - Outcome was fully passing after adapting old fixture fields.

## Files changed for this step

- `demo/src/teachgrav/entry.py`
- `demo/src/teachgrav/viz.py`
- `demo/tests/teachgrav/test_convergence_video.py`

## Outcome

The codebase now has unified fps argument wiring for both video creation paths, with regression tests updated and passing. The remaining plan phases (target-frame interpolation/upsampling and related tests) are intentionally deferred to subsequent commits.
