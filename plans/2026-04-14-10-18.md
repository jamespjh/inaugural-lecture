# Plan: Add plot of comparison of various numerical engines (Issue #46)

## Date: 2026-04-14

## Summary

Add a benchmark figure to slide 1.2.2 that compares the gravity law evaluation
performance across available numerical engines as a function of the number of
bodies, using log–log axes.

## Requirements (from issue #46)

- Use the array benchmarking code (`teachgrav.benchmark`)
- Use `scatter` scenario with a fixed seed (42)
- One axis is the engine (each engine = one line)
- One axis is the number of bodies – from 2 to 1024
- Log–log on both axes
- Style inspired by https://github.com/jamespjh/stubs/blob/main/languages/python/multiply/src/multiply/notebooks/apple.ipynb
- Insert into slide 1.2.2 in place of the placeholder

Note: seeds will be inconsistent across engines for now; this is tracked in issue #48.

## Changes

### `demo/src/teachgrav/benchmark.py`
- Add `benchmark_range(fn_at_size, sizes, engine=None)` helper that sweeps a
  callable over an iterable of sizes and returns `[(size, time), ...]`.

### `demo/src/teachgrav/notebooks/benchmark_engines.ipynb`
- New Jupyter notebook that:
  - Discovers available engines (excluding `python` and `numba`)
  - Benchmarks the `gravity` law on the `scatter` scenario with seed=42
  - Sweeps n_bodies from 2 to 1024 (powers of 2)
  - Produces a log–log plot and saves it to `slideshow/assets/benchmark_engines.png`

### `slideshow/assets/benchmark_engines.png`
- Pre-generated figure committed alongside the notebook so the slideshow
  renders without requiring a live benchmark run.

### `slideshow/part1.qmd`
- Section 1.2.2: replace `{{< video-credit src="assets/placeholder.png" ... >}}`
  with `{{< image-credit src="assets/benchmark_engines.png" ... >}}`.
