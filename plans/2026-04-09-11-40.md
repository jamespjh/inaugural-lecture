## Plan: Decouple Solver and Law + 1.1.3 Constant Motion Asset

Implement a law-selection layer end-to-end (CLI -> execution -> integrator) so solver choice is independent of physics law choice, add a true constant-velocity law, enforce fitted-law solver compatibility by warning and auto-switching to Euler, and wire the generated constant-motion video into slide 1.1.3.

**Steps**
1. Phase 1: Introduce law selection in CLI and execution path.
2. In /Users/jamespjh/devel/inaugural/demo/src/teachgrav/entry.py add a new `--law` argument with names: `gravity`, `constant`, `gaussian`, `power`.
3. In /Users/jamespjh/devel/inaugural/demo/src/teachgrav/entry.py update `solve(...)` and its call sites to pass selected law through to the integrator.
4. Add parser-level compatibility behavior in /Users/jamespjh/devel/inaugural/demo/src/teachgrav/entry.py: when law is `gaussian` or `power` and method is not `euler`, emit a clear warning and auto-set method to `euler` (as requested).
5. Phase 2: Add law factory + constant-velocity law model.
6. Create /Users/jamespjh/devel/inaugural/demo/src/teachgrav/laws/constant_law.py implementing a model with derivative rules: position derivative equals velocity, velocity derivative equals zero acceleration, and immobile masking matches existing law behavior.
7. In /Users/jamespjh/devel/inaugural/demo/src/teachgrav/laws/laws.py add a small law-construction helper (string name -> model instance).
8. Phase 3: Add new 'single' scenario.
9. In /Users/jamespjh/devel/inaugural/demo/src/teachgrav/scenarios.py add `init_single_body()` method returning a single-body system with position and velocity, WITHOUT normalizing for centre of mass.
10. Add `'single'` to the dispatch dict in `ScenarioFactory.create_scenario()`.
11. Phase 4: Make integrators consume selected law instead of hardcoded true law.
12. In /Users/jamespjh/devel/inaugural/demo/src/teachgrav/integrator.py replace default `model=TrueLawModel()` behavior with explicit law/model construction from selected law name and scenario factory context.
13. Phase 5: Add slide 1.1.3 generation wiring for constant-motion demo.
14. In /Users/jamespjh/devel/inaugural/slideshow/assets/scenarios.yml add an entry for slide 1.1.3 output using the new `single` scenario + law constant.
15. In /Users/jamespjh/devel/inaugural/slideshow/part1.qmd section 1.1.3 replace placeholder source `assets/placeholder.mp4` with `assets/constant_euler.mp4` and update descriptive text/aria-label accordingly.
16. Phase 6: Test coverage updates for CLI and integration behavior.
17. Extend /Users/jamespjh/devel/inaugural/demo/tests/teachgrav/test_entry.py, /Users/jamespjh/devel/inaugural/demo/tests/teachgrav/test_integrator.py, /Users/jamespjh/devel/inaugural/demo/tests/teachgrav/test_scenarios.py and add /Users/jamespjh/devel/inaugural/demo/tests/teachgrav/test_constant_law.py.

**Verification**
1. Run targeted tests for entry/integrator/law updates using explicit demo venv executables.
2. Generate slideshow assets from /Users/jamespjh/devel/inaugural/slideshow/assets and confirm `constant_euler.mp4` exists.
3. Render slides and confirm 1.1.3 uses `assets/constant_euler.mp4`.
4. Validate fitted-law requests such as `--law gaussian --method LSODA` normalize to Euler with warning.

**Decisions**
- Law names for MVP: `gravity`, `constant`, `gaussian`, `power`.
- Fitted laws with non-Euler methods: warn and auto-switch to Euler.
- Added a dedicated `single` scenario to support deterministic constant-motion demo generation.
- Added unit/integration coverage around law parsing, normalization, constant derivative semantics, and trajectory expectations.