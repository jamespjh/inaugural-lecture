# Plan: Issue #4 Euler vs RK45 Trail Slide

## Goal

Implement issue #4 by generating two trail-mode sun-orbit assets (Euler and RK45), replacing the current placeholder media in section 1.1.6 of `slideshow/part1.qmd`.

## Scope decision

Compare Euler vs RK45 (user revised from original LSODA to RK45).
Replace existing 1.1.6 media in-place (no slide split).

## Steps

### Phase 0: Issue workflow setup
1. Create branch `feature/trail-slide-euler-rk45` from `main` ✓
2. Add this plan record under `plans/2026-04-09-12-00.md` ✓
3. Post plan as comment on GitHub issue #4 with AI metadata

### Phase 1: Asset generation configuration
4. Update `slideshow/assets/scenarios.yml` — add two entries:
   - `scenario: sun`, `method: euler`, `visualise: trail`, `duration: 5`, `outfile: sun_euler_trail.mp4`
   - `scenario: sun`, `method: RK45`, `visualise: trail`, `duration: 5`, `outfile: sun_RK45_trail.mp4`

### Phase 2: Slide wiring
5. Update section 1.1.6 in `slideshow/part1.qmd`:
   - Replace `assets/sun_euler_path.mp4` → `assets/sun_euler_trail.mp4`
   - Replace `assets/sun_RK4_path.mp4` → `assets/sun_RK45_trail.mp4`
   - Update alt text to reflect Euler drift vs RK45 closed orbit narrative
   - Preserve `.two-col` + `video-credit` shortcode layout

### Phase 3: Generate and validate
6. Generate assets from `slideshow/assets/` using demo venv `generate-figures scenarios.yml`
7. Confirm both `sun_euler_trail.mp4` and `sun_RK45_trail.mp4` exist
8. Render `slideshow/part1.qmd` and confirm HTML references new assets

### Phase 4: Commit/PR hygiene
9. Add `prompts/2026-04-09-12-00.md`
10. Commit with AI trailer, push, open PR `Closes #4`, post AI-metadata comment on issue #4

## Relevant files

- `slideshow/assets/scenarios.yml` — add 2 trail entries
- `slideshow/part1.qmd` — update 1.1.6 src paths and alt text

## Verification

1. Both trail mp4s generated and present in `slideshow/assets/`
2. Rendered HTML references `sun_euler_trail.mp4` and `sun_RK45_trail.mp4`
3. Section 1.1.6 narrative: Euler non-closing trajectory vs RK45 closed orbit
